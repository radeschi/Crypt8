export type LocaleId = "pt-BR" | "en" | "es" | "de" | "fr" | "ja" | "zh-CN";

export interface Messages {
  dropHere: string;
  dropRelease: string;
  chooseFile: string;
  password: string;
  confirmPassword: string;
  show: string;
  hide: string;
  encrypt: string;
  cancel: string;
  replace: string;
  keep: string;
  encrypting: string;
  decrypting: string;
  calculating: string;
  finalizing: string;
  seconds: (count: number) => string;
  minutes: (count: number) => string;
  hours: (count: number) => string;
  encrypted: string;
  another: string;
  again: string;
  mismatch: string;
  emptyPassword: string;
  decryptAsk: string;
  decrypt: string;
  open: string;
  saveAs: string;
  chooseWhere: string;
  restored: string;
  opened: string;
  saved: string;
  tempHint: string;
  reveal: Record<"finder" | "explorer" | "folder", string>;
  files: (count: number) => string;
  folders: (count: number) => string;
  items: (count: number) => string;
  aboutMenu: string;
  versionLabel: (version: string) => string;
  tagline: string;
  aboutSummary: string;
  openSource: string;
  websiteLink: string;
  licenseLink: string;
  githubLink: string;
  errors: Record<string, string>;
}
